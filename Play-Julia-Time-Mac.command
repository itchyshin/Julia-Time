#!/bin/zsh
# Double-click this file to play Julia Time on a Mac. The first time, it also runs the one-time
# setup (a few minutes). Keep the Terminal window open while you play.
exec /bin/zsh "${0:A:h}/tools/setup/launch-macos.command"
