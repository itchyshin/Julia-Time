# run state, never a deliverable
.unlazy/
.unlazy-hook-state.json
LOOP/state/
# Julia
*.jl.cov
*.jl.*.cov
*.jl.mem
/docs/build/
# OS / editors
.DS_Store
.vscode/

# graft's local graph cache — regenerable, not committed (run `graft build`).
/graft/
